```python
from module import *      
# from …… import : 导入一个模块中的一个函数，相当于导入的是一个文件夹中的文件，是个绝对路径。
# 把一个模块中的所有函数都导入进来，相当于导入的是一个文件中所有文件，所有函数都是绝对路径
# 直接使用函数就行了
# 跳过私有属性
# 会导入xxx中已导入的函数
# 

import random
# 相对路径，使用的时候需要确定是哪个模块




```

